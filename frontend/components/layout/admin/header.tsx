import React from "react";

const AdminHeader = () => {
    return (
        <header style={{ background: "#222", color: "#fff", padding: "1rem 0", textAlign: "center" }}>
            <h1>Admin Dashboard</h1>
        </header>
    );
};

export default AdminHeader;